### Build Error: Task `runPluginVerifier` Not Found

**Observed Behavior:**
The build fails during the GitHub Actions workflow with the following error:

    Task 'runPluginVerifier' not found in root project 'jetbrains-krzysztof-plugin'.

**Expected Behavior:**
The build should complete successfully, executing the `runPluginVerifier` task as part of the plugin verification process.

### Steps to Fix:
1. Open the `build.gradle.kts` file in the project.
2. Add the following configuration to define the `runPluginVerifier` task:
   ```kotlin
   import org.jetbrains.intellij.IntellijPluginVerifierTask
   
   tasks.register<IntellijPluginVerifierTask>("runPluginVerifier") {
       // Configure plugin verifier task options here
       setPluginName("your-plugin-name")
       // Other necessary configurations
   }
   ```
3. Save the changes and commit the updated `build.gradle.kts` file.
4. Push the changes to the repository and trigger a new build on GitHub Actions to verify the fix.

Refer to the [IntelliJ Plugin Verifier documentation](https://plugins.jetbrains.com/docs/intellij/plugin-verifier.html) for more details on configuring the plugin verifier task. 

### Additional Information:
- **Date & Time of Issue:** 2026-04-01 21:29:53 UTC
- **User:** krzysbaranski